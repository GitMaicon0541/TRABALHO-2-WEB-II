<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editar Usuário</title>
</head>
<body>
  <h1>Editar Usuário</h1>
  <form action="/<%= user.id %>?_method=PUT" method="POST">
    <input type="hidden" name="_method" value="PUT">
    <label for="nome">Nome:</label>
    <input type="text" id="nome" name="nome" value="<%= user.nome %>" required <% if (user.tipo === 'admin') { %> disabled <% } %>>
    <label for="cpf">CPF:</label>
    <input type="text" id="cpf" name="cpf" value="<%= user.cpf %>" required <% if (user.tipo === 'admin') { %> disabled <% } %>>
    
    <% if (user.tipo === 'admin') { %>
      <!-- Usuários com perfil ADMIN só podem editar telefones -->
      <label for="telefone">Telefone:</label>
      <input type="text" id="telefone" name="telefone" value="<%= user.telefone %>" required>
      <!-- O campo de email será apenas visualizado -->
      <label for="email">Email:</label>
      <input type="email" id="email" name="email" value="<%= user.email %>" readonly>
    <% } else { %>
      <!-- Outros tipos de usuários podem editar todos os campos -->
      <label for="telefone">Telefone:</label>
      <input type="text" id="telefone" name="telefone" value="<%= user.telefone %>" required>
      <label for="email">Email:</label>
      <input type="email" id="email" name="email" value="<%= user.email %>" required>
    <% } %>

    <!-- O campo tipo não será editável -->
    <input type="hidden" id="tipo" name="tipo" value="<%= user.tipo %>">

    <!-- Adicionando o campo de entrada oculto para enviar o ID do usuário -->
    <input type="hidden" name="id" value="<%= user.id %>">

    <button type="submit">Salvar Alterações</button>
  </form>
  <a href="/">Cancelar</a>
</body>
</html>

